# Controllers (incremental split from app.js)

- `app.js` — bootstrap + WS + screen routing (shrinking over time)
- `../render/hand-render.js` — diffed hand DOM
- `../interactions/hand-focus.js` — one-thumb tap focus
- `../interactions/hand-drag-ask.js` — optional drag-to-ask

Next extractions: `lobby.js`, `table.js`, `sheets.js` under `render/`.
